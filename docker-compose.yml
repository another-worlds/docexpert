
version: '3.8'

services:
  llm-telegram-bot:
    build: .
    container_name: llm-telegram-bot
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - XAI_API_KEY=${XAI_API_KEY}
      - HUGGINGFACE_API_KEY=${HUGGINGFACE_API_KEY}
      - MONGODB_URI=${MONGODB_URI}
      - MONGODB_DB_NAME=${MONGODB_DB_NAME}
      - EMBEDDING_SERVICE=${EMBEDDING_SERVICE:-huggingface}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - LOGS_DIR=/app/logs
    volumes:
      - ./uploads:/app/uploads
      - ./logs:/app/logs:rw
  - llm_telegram_bot_data:/app/data
    env_file:
      - .env
    networks:
      - llm-telegram-bot-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

volumes:
  llm_telegram_bot_data:

networks:
  llm-telegram-bot-network:
    driver: bridge
